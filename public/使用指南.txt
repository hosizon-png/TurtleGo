您好！

我们已经成功为您创建了 `/public` 静态资源目录。

为了在起始页面上使用您精美绘制的龟小旅图片，请按照以下简单步骤操作：

1. 在左侧面板的【文件浏览器】（File Explorer）中，找到新创建的 `public` 文件夹。
2. 将您本地的 `turtle_mascot.png` 拖拽或上传到这个 `public` 文件夹中，确保文件名完全为：
   `turtle_mascot.png`

程序将会自动读取该图片的绝对路径 `/turtle_mascot.png` 并在新起始界面中完美显示它！
